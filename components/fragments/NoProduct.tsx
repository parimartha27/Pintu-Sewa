const NoProduct = ({ message }: { message: string }) => {
  return <p className='text-center text-color-secondary font-semibold text-lg md:text-2xl w-full mt-6'>{message}</p>
}

export default NoProduct
